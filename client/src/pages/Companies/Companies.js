import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const Companies = () => {
  return (
    <div>
      <Title level={2}>Companies</Title>
      <p>Companies management page - Coming soon!</p>
    </div>
  );
};

export default Companies;